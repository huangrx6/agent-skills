# Local Excalidraw libraries

- `user/` — put your own `.excalidrawlib` files here.
- `official/` — populated by `draw.mjs library install ...` from the public Excalidraw libraries catalog.

The compiler searches item names, text content, and library filenames.
